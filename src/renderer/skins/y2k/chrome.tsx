import type { SkinChrome } from '../types';
import { YkBackdrop } from './YkBackdrop';

/** Y2K Chrome replaces the sky behind the panes. */
const chrome: SkinChrome = {
	Backdrop: YkBackdrop,
};

export default chrome;
